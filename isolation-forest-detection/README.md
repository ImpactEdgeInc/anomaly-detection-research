The requirements to run the contents of the folder are the same as in the `lstm-testing` folder.

The `module` folder contains the modularized files that allow you to run the anomaly detection code on a submitted file or table, and is probably what you are looking for. The rest of this folder contains notebooks that test the application of this code on certain tables, and include additional features that did not end up being used in the modulee for various reasons.
